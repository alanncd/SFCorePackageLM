trigger RinCentralTaskTrigger on Task (after insert) {
    /*if(trigger.isBefore) {
        if(trigger.isInsert) {
          TaskTriggerHandler2.beforeInsert(trigger.New);
          
        }
    }*/
    //if(RingCentralTaskSetting__c.getOrgDefaults().Activate_Trigger__c) {
    //	if(trigger.isAfter) {
	   //     if(trigger.isInsert) {
	     //       RingCentralDataStoreHandler.onAfterInsertTask(trigger.newMap);  
	    	   // }
		   // }
        //}
}